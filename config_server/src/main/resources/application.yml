server:
  port: 8088
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Danaxar/mtisw2_config_server
          search-paths: config-data
  application:
    name: config-microservice